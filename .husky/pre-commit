# Pre-commit hook — runs on every `git commit`.
#
# lint-staged runs fixers against only the files staged for this commit and
# re-stages its own fixes without sweeping in unrelated unstaged changes.

npm run lint-staged
